# CHANGELOG

这个文件记录版本级别的变更。

日常提交日志见 `docs/开发日志.md`。

## V2.1.0 · 2026-05-07

轻量自动识别和提醒版本。

### 新增

- 设置页新增 Remind 页面
- 设置页新增 Auto 页面
- 支持按状态标签配置超时提醒和稍后提醒
- 支持按前台 App 名称或 Bundle ID 绑定状态标签
- Auto 页面改为左侧标签、右侧运行 App 的可视化拖拽绑定面板
- 运行 App 卡片显示 App 图标、绑定状态和标签色点
- Auto 面板左右列表改为独立滚动，避免长列表拖拽时错位
- App 卡片新增绑定菜单，可不拖拽直接选择目标标签
- 支持 App 命中后稳定一段时间再自动切换
- 自动记录新增 `app_name` 和 `app_bundle_id` 字段
- 当前状态和历史列表显示为 `标签 - App`，例如 `看消息 - 飞书`
- 同一标签下切换不同绑定 App 时也会生成新记录
- 自动记录写入 `source=auto_app_rule`
- 自动记录补充 `source_detail`，记录命中 App 和规则原因
- Excel 导出新增 `source_detail` 字段

### 保护

- 手动记录后默认进入 3 分钟冷却，避免自动切换立刻覆盖人工选择
- 自动识别只使用 macOS 前台 App 激活事件，不做截图、录屏或 OCR
- `data/`、本地导出、构建缓存和 `.app` 包不进入仓库

### 调整

- 设置页保留在浮标面板内打开，取消独立可缩放窗口方案

## V1.0.0 · 2026-05-06

首个公开可用版本。

### 新增

- macOS 顶栏常驻入口
- 桌面悬浮浮标入口
- 主界面核心流程，按钮、当前状态、持续时间
- 点击按钮后展开标签选择
- 标签新增、修改、删除
- 设置页拆分为 color、Add、Export
- 本地 JSON 存储
- JSON 和 Excel 一键导出
- 今天记录展开查看
- 翻页时钟显示

### 调整

- 主界面精简为最小操作面
- style 代码保留，前台入口隐藏
- 悬浮浮标误触范围缩小

### 修复

- 撤销改为撤销当前最新一条
- 标签新增后的颜色分配问题
- 设置页无法打开的问题
- 翻页时钟重叠和容器对齐问题
