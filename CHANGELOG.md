# CHANGELOG

这个文件记录版本级别的变更。

日常提交日志见 `docs/开发日志.md`。

## 未发布 · 2026-05-12

### 调整

- Auto 圆环倒计时刷新率从 20fps 提升到 45fps，改善走圈动画的顺滑度

## test 2.3.0 · 2026-05-07

Auto 体验和发布稳定性测试版本。

### 新增

- Auto 自动切换后支持浮标气泡和系统通知反馈
- 悬浮浮标支持 Auto 倒计时显示，可选底条、圆环、水位或关闭
- 顶部新增圆形导出按钮，支持导出今天或全部记录
- Auto 绑定缓存 App 显示名，App 退出后仍优先显示中文名称
- 文本输入 Bundle ID 绑定时也会缓存已知 App 显示名

### 调整

- 设置页移除扫描环和列表高亮两个 Auto 反馈入口，保留更轻量的气泡和系统通知
- 主界面顶部按钮尺寸、位置和导出按钮视觉统一
- Auto 绑定列表滚动条隐藏，减少遮挡
- 打包脚本每次重建 `.app`，并清理 Finder/FileProvider 元数据后再签名

### 修复

- 修复绑定 App 退出后显示为 `com.xxx...` 的问题
- 修复解绑时显示名缓存大小写不一致导致残留的问题
- 修复导出菜单潜在 NSCoder 初始化崩溃点
- 修复 `.app` 严格签名校验可能被 Finder 元数据打断的问题

### 隐私

- 自动识别仍只使用 macOS 前台 App 激活事件和运行 App 列表
- 不采集截图、录屏、OCR、窗口内容、键盘输入或剪贴板内容
- `data/`、本地导出、构建缓存、发布包和归档目录继续保持忽略，不进入仓库

## V2.1.0 · 2026-05-07

轻量自动识别和提醒版本。

### 新增

- 设置页新增 Remind 页面
- 设置页新增 Auto 页面
- 支持按状态标签配置超时提醒和稍后提醒
- 支持按前台 App 名称或 Bundle ID 绑定状态标签
- Auto 页面改为左侧标签、右侧运行 App 的可视化拖拽绑定面板
- 运行 App 卡片显示 App 图标、绑定状态和标签色点
- Auto 面板左右列表改为独立滚动，避免长列表拖拽时错位
- App 卡片新增绑定菜单，可不拖拽直接选择目标标签
- 支持 App 命中后稳定一段时间再自动切换
- 自动记录新增 `app_name` 和 `app_bundle_id` 字段
- 当前状态和历史列表显示为 `标签 - App`，例如 `看消息 - 飞书`
- 同一标签下切换不同绑定 App 时也会生成新记录
- 自动记录写入 `source=auto_app_rule`
- 自动记录补充 `source_detail`，记录命中 App 和规则原因
- Excel 导出新增 `source_detail` 字段

### 保护

- 手动记录后默认进入 3 分钟冷却，避免自动切换立刻覆盖人工选择
- 自动识别只使用 macOS 前台 App 激活事件，不做截图、录屏或 OCR
- `data/`、本地导出、构建缓存和 `.app` 包不进入仓库

### 调整

- 设置页保留在浮标面板内打开，取消独立可缩放窗口方案

## V1.0.0 · 2026-05-06

首个公开可用版本。

### 新增

- macOS 顶栏常驻入口
- 桌面悬浮浮标入口
- 主界面核心流程，按钮、当前状态、持续时间
- 点击按钮后展开标签选择
- 标签新增、修改、删除
- 设置页拆分为 color、Add、Export
- 本地 JSON 存储
- JSON 和 Excel 一键导出
- 今天记录展开查看
- 翻页时钟显示

### 调整

- 主界面精简为最小操作面
- style 代码保留，前台入口隐藏
- 悬浮浮标误触范围缩小

### 修复

- 撤销改为撤销当前最新一条
- 标签新增后的颜色分配问题
- 设置页无法打开的问题
- 翻页时钟重叠和容器对齐问题
