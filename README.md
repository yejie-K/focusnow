# 注意力切换记录软件

当前主程序是一个 macOS 顶栏常驻工具，不再走旧的 Python 窗口入口。

运行入口有两个：

- 直接打开打包后的 `状态切换工具.app`
- 或在项目目录执行 `swift run`

启动后不会出现传统窗口。入口在系统 topbar 的彩色圆环图标，另有每块屏幕右侧中部的悬浮浮标，鼠标靠近会展开面板。

主界面现在只保留按钮、当前状态、持续时间。点击按钮后才展开标签选择。标签管理、外观切换、颜色和风格选择都在设置页。
